import { ClipLoader } from "react-spinners";

const LoadingSpinner = () => {
  return (
    <div className="z-20">
      <ClipLoader color="#d7d0ba" />
    </div>
  );
};

export default LoadingSpinner;
